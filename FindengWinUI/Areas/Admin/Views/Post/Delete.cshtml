@model Post
@{
    ViewData["Title"] = "Blog Sil";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">
    <div class="card-body">
        <form asp-action="Delete" asp-controller="Post" method="post">
            <p>
                @Model.Title isimli makaleyi silmek istediğinizden emin misiniz?
            </p>
            <button class="btn btn-danger">Evet</button>
            <a asp-action="Index" asp-controller="Post" class="btn">İptal</a>
        </form>
    </div>
</div>
